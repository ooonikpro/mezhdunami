{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "start": "nuxt preview",
    "docker-mongo": "docker compose run mongo",
    "docker-build": "docker compose build",
    "docker-start": "docker compose down && yarn docker-build && docker compose up"
  },
  "devDependencies": {
    "@nuxtjs/pwa": "^3.3.5",
    "@types/node": "^18.11.2",
    "nuxt": "3.0.0-rc.11",
    "sass": "^1.55.0",
    "sass-loader": "^13.0.2"
  },
  "dependencies": {
    "mongodb": "^4.10.0"
  }
}
